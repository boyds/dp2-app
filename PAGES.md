Add product (simple shim: name + category)
    -> redirects to edit product

Edit product (has #id, description)

Add category (name)
    -> redirects to edit category

Edit category (has #id, description)
